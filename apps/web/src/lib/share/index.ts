// 공유 공통 모듈 배럴 — portable 순수 유틸(shareOrCopy)만 벤더링한다.
// 버튼은 앱 디자인 토큰(Button)·토스트(sonner)에 맞춘 ShareButton(components/feature)을 쓴다.
export { shareOrCopy } from './shareOrCopy'
export type { ShareInput, ShareResult } from './shareOrCopy'
