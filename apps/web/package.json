{
  "name": "@desk/web",
  "version": "0.1.0",
  "private": true,
  "description": "@desk/platform 웹 — 가격(pricing) + 테넌트 빌링 대시보드. (Stage 1: 스캐폴드 + pricing 표)",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@desk/shared": "workspace:*",
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  },
  "devDependencies": {
    "@types/react": "^19.2.0",
    "@types/react-dom": "^19.2.0",
    "@vitejs/plugin-react": "^5.1.0",
    "typescript": "^6.0.3",
    "vite": "^7.1.12",
    "vitest": "^4.1.8"
  }
}
